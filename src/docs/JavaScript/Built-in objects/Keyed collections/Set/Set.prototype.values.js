/**
 * Set.prototype.values()
 * 返回一个新的 Iterator 对象，该对象包含 Set 对象中的按插入顺序排列的所有元素的值。
 * Set.prototype.keys() (en-US)
 * keys() 方法是这个方法的别名（与 Map 对象相似）；他们的行为一致，都是返回Set 对象中的元素值。
 */

/*
const set = new Set(['A', 'B', 'C']);
const newSet = set.entries();
const newSetValues = set.values();
const newSetKeys = set.keys();
console.log(newSet.next().value); // [ 'A', 'A' ]
console.log(newSetValues.next().value); // A
console.log(newSetKeys.next().value); // A

console.log(newSet1); // [Set Iterator] { 'A', 'B', 'C' }
*/
