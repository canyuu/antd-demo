/**
 * 使 set转换为 iterator 结构 可使用 iterator 接口相关API
 */

// 返回 Set iterator 函数，默认值是 values() 函数。
/*
const set1 = new Set(['A', 'B', 'C']);
const iterator1 = set1[Symbol.iterator]();
console.log(iterator1.next().value);
console.log(iterator1.next().value);
*/

